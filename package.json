{
  "name": "dji-ev50-vtol",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm --prefix threejs run build",
    "replay:inspect": "python scripts/replay_log.py models/SITL_optimal.ulg --inspect",
    "replay:build": "python scripts/replay_log.py models/SITL_optimal.ulg threejs/public/flight-replay.json",
    "test": "npm --prefix threejs run test:api && npm --prefix threejs run test:presentation && npm --prefix threejs run test:flight && npm --prefix threejs run test:model && npm --prefix threejs run test:airframe",
    "ci": "npm run format:check && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.7"
  }
}
