from pathlib import Path
p=Path('threejs/src/environment.ts');s=p.read_text(encoding='utf-8');s=s.replace("'#include <common>\\nvarying vec3 vRock;').replace('#include <color_fragment>'", "'#include <common>\\nvarying vec3 vRock;\\nfloat hashRock(vec3 p){return fract(sin(dot(p,vec3(127.1,311.7,74.7)))*43758.5453);}\\nfloat rockNoise(vec3 p){vec3 i=floor(p),f=fract(p);f=f*f*(3.0-2.0*f);return mix(mix(mix(hashRock(i),hashRock(i+vec3(1,0,0)),f.x),mix(hashRock(i+vec3(0,1,0)),hashRock(i+vec3(1,1,0)),f.x),f.y),mix(mix(hashRock(i+vec3(0,0,1)),hashRock(i+vec3(1,0,1)),f.x),mix(hashRock(i+vec3(0,1,1)),hashRock(i+vec3(1,1,1)),f.x),f.y),f.z);}').replace('#include <color_fragment>'")
s=s.replace('float strata=sin(vRock.y*1.7+sin(vRock.x*.21)*3.0+sin(vRock.z*.27)*2.0);float grit=sin(vRock.x*2.4+vRock.z*1.8)*sin(vRock.y*3.7);diffuseColor.rgb*=0.89+0.09*strata+0.035*grit;', 'float strata=rockNoise(vRock*.08)*.5+rockNoise(vRock*.27)*.3+rockNoise(vRock*.8)*.2;diffuseColor.rgb*=.72+.42*strata;')
p.write_text(s,encoding='utf-8')
